

async function topTenStoresFromCache() {
    // sortedlist member => storeId, score => ranking
    // using the storeId get the json
    // review updated => is the top ten changed? replace the old one by the new in the ranking

}